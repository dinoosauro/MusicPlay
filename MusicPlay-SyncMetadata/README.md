# MusicPlay-SyncMetadata

This directory contains the C# Blazor WebAssembly project used to merge the edited metadata from MusicPlay with the output music file. This functionality is used only when the user asks to download the uploaded audio file, and the user must manually request to merge them.