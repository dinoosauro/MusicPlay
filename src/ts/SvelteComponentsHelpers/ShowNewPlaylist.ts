import type { PlaylistContainer } from "../Player/PlayerInterfaces";

export default {
    /**
     * Function called to add a playlist to the "Playlist" list viewer
     */
    showPlaylist: undefined as ((item: PlaylistContainer) => void) | undefined
}